target/
launcher/gen/schemas/
disk.img

# Populated by .github/scripts/fetch-*; never checked in.
launcher/binaries/
launcher/firmware/
launcher/qemu-libs/

# Written by the release workflow when it merges in the signing identity.
launcher/tauri.signed.conf.json
