[package]
name = "helloware"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "time", "io-util"] }
axum = { version = "0.7", features = ["ws"] }
futures-util = "0.3"
